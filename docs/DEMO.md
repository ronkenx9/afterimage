# Demo

1. Run `pnpm dev` and open `http://localhost:3000`.
2. Select **Use replay case**.
3. Open the known-basis result and inspect its evidence hashes.
4. Follow the chronological trace, including the incoming unknown-basis transfer and unsupported LP event.
5. Compare the observed Solana entry with the current Binance Spot snapshot.
6. Enter 50 USDT, preview the immutable paper intent, approve it, and show the reconciled paper receipt.
7. State clearly that the replay is synthetic, the market adapter is public REST, and authenticated Agent OS execution is pending.

CLI fallback: `pnpm demo:replay`.
