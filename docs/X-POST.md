# Submission tweet

Post as a **quote-tweet of the official Binance Agent OS hackathon announcement**.
Tag `@binance` and use `#AgentOS #BinanceAgentOS`. Repo link:
`github.com/ronkenx9/afterimage`.

## Option A — concise single tweet (< 280)

AFTERIMAGE — an autonomous agent on Binance Agent OS.

It surfaces its own Spot trades from live data, and never proposes one without the undo computed: unwind order, stop, cost to reverse.

Bound to the real Agent OS tools/list. Never spends your funds.

github.com/ronkenx9/afterimage

## Option B — thread (covers every feature)

**1/**
AFTERIMAGE — an autonomous trading agent built on Binance Agent OS.

It reads live Spot markets, surfaces its own trades, and refuses to propose one without the exit already computed.

An agent that finds an edge — and always leaves you the way back. 🧵

**2/**
The edge is reversibility. Every trade it drafts ships with its exit pre-computed: the inverse unwind order, a protective stop, the round-trip cost, and a time-boxed auto-unwind.

It never hands you a position without the way out.

**3/**
It learns. From ~240 real candles per symbol it measures what each setup actually did — a breakout's hit-rate, its expectancy, and when it was a trap.

Proposals cite that history. A memory of what worked — not a black-box predictor.

**4/**
It can't run wild. Guardrails are enforced in code at one chokepoint nothing it reads can override: no withdrawals, ever. Spot only. Hard caps. And it never self-executes.

Plus live on-chain reconstruction (Solana), and it runs 24/7 as a daemon.

**5/**
How it uses Agent OS: 13/14 capabilities bound to the authenticated tools/list — no guessed tool names — with live Spot reads through Agent OS, and every write routed through it behind an owner approval.

Built on @binance Agent OS.
github.com/ronkenx9/afterimage
